version: "3.9"

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: truthbite-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
    volumes:
      - ./data/qdrant_storage:/qdrant/storage
